<h5>Projects that require you to submit your hearing dates and recommendations</h5>
{{#each this.model as |project|}}
  <div class="project-summary-card callout">
    <div class="grid-x">
      <div class="cell large-6">
        <h3 class="tiny-margin-bottom">
          <a>{{project.dcp_projectname}}</a>
          <small class="dark-gray">{{project.ulurp_nonulurp}}</small>
        </h3>
        <h5 class="applicant">{{project.applicants}}.</h5>
        <p class="no-margin">{{project.dcp_projectbrief}}.</p>
      </div>
      <div class="cell large-shrink">
        <div class="lup-countdown">
          <p class="cert-date">Certified 11/1/2018</p>
          <p class="days">8</p>
          <p class="total">of 60 days remain</p>
          <p class="end-date">Ends 12/15/19</p>
        </div>
      </div>
      <div class="cell large-auto">
        <a class="button expanded no-margin">Add Your<br>Hearing&nbsp;Date</a>
        {{#project-recommendations
          project=project
          user=(await this.currentUser.user)
          as |projectRecommendations|
        }}
          {{#each projectRecommendations.list as |projectRecommendation|}}
            <LinkTo @route="my-projects.project.recommendations.add"
              @model={{projectRecommendation}}
              class="button expanded no-margin"
            >
              Submit&nbsp;Your<br>
              <b>
              {{#if (eq projectRecommendation.constructor.modelName "borough-president-recommendation")}}
                Borough&nbsp;President
              {{else if (eq projectRecommendation.constructor.modelName "borough-board-recommendation")}}
                Borough&nbsp;Board
              {{else if (eq projectRecommendation.constructor.modelName "community-board-recommendation")}}
                Community&nbsp;Board
              {{/if}}
              </b>
              Recommendation
            </LinkTo>
          {{/each}}
        {{/project-recommendations}}
      </div>
    </div>
  </div>
{{/each}}

{{outlet}}
